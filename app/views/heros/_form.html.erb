<%= form_with(model: hero, local: true) do |form| %>
  <% if hero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hero.errors.count, "error") %> prohibited this hero from being saved:</h2>

      <ul>
      <% hero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :hero_name %>
  </div>

  <div class="field">
    <%= form.label :code_sample %>
    <%= form.text_field :code_sample, id: :hero_code_sample %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :hero_text %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :hero_color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
